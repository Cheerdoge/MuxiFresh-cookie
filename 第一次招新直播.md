# 访问网站的本质
所谓在网站所做的登录、下载文件、查看文章等动作，都可以概括为**请求-响应**结构
比如查看文章，电脑向网站服务器发请求说：我要看这篇文章，服务器收到请求，返回响应，包含这篇文章
以登录为例，我们的电脑向服务器发起登录请求，服务器收到请求，返回响应，包含cookie

## 请求
基本结构如下
请求行
请求头
空行
请求体（可选）

## 响应
基本结构如下
状态行
响应头
空行
响应体（可选）
# Cookie
## 什么是Cookie
**Cookie 是 HTTP 用来“记住状态”的一种机制**。

## Cookie的作用
让服务器知道登陆的用户身份

## Cookie的性质
包含用户身份
有时间限制

# 爬虫
## 什么是爬虫
**网络爬虫**是一种自动化程序，能够模拟用户浏览器行为，向目标网站发送请求，获取网页内容并提取有用信息。

## 原理
服务器返回的响应包含了需要的内容

## 流程
爬虫程序向目标服务器发起请求-->目标服务器返回响应-->爬虫程序自动从响应中获取需要的内容-->爬虫完成

# 神秘小按钮F12
打开F12，可以打开控制台，能查看元素、网络、手机版网站等
## 元素
在这里能看到网站内的内容

## 网络
能查看请求-响应

# 看一下用go发起一个http请求的代码
```go
package main
import (
	"fmt"
	"io"
	"net/http"
	"os"
	"time"
)
func main() {
	// 1. 设置目标网址
	url := "https://www.example.com"
	// 2. 创建一个带超时的 Client
	//    不用 http.Get 是因为它没有超时，网络不好时会一直卡住
	client := &http.Client{
		Timeout: 10 * time.Second,
	}
	// 3. 创建请求
	req, err := http.NewRequest(http.MethodGet, url, nil)
	if err != nil {
		fmt.Println("创建请求失败:", err)
		os.Exit(1)
	}
	// 4. 添加请求头（有些网站会检查 User-Agent，没有会被拒绝）
	req.Header.Set("User-Agent", "Mozilla/5.0 (Go learning bot)")
	// 5. 发起请求
	resp, err := client.Do(req)
	if err != nil {
		fmt.Println("请求失败:", err)
		os.Exit(1)
	}
	// 6. 一定要记得关闭响应体，否则会泄漏连接
	defer resp.Body.Close()
	// 7. 查看状态码
	fmt.Println("状态码:", resp.StatusCode, resp.Status)
	// 8. 读取响应内容
	body, err := io.ReadAll(resp.Body)
	if err != nil {
		fmt.Println("读取响应失败:", err)
		os.Exit(1)
	}
	// 9. 打印前 500 个字符，防止输出太长
	if len(body) > 500 {
		fmt.Println(string(body[:500]))
	} else {
		fmt.Println(string(body))
	}
```