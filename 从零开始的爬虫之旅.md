# 1. 访问网站的本质
所谓在网站所做的登录、下载文件、查看文章等动作，都可以概括为**请求-响应**结构
![[image/请求-响应结构.png]]
## 1.1登录
电脑发请求说：这是我的**账号和密码**，我要**登录**
服务器**收到请求**，返回响应：账号密码**正确**，这是你的**cookie**
## 1.2 查看公开文章
电脑发请求：我是用户A，这是我的**cookie**，我要看这个**公开文章**
服务器**收到请求**，返回响应：**cookie解析完成，你是用户A**，文章在这
## 1.3查看私密文章
电脑发请求：我是用户A，这是我的**cookie**，我要看我的**私密文章**
服务器收到请求，返回响应：**cookie解析完成，你是用户A**，你拥有查看该文章的**权限**，文章在这

## 1.4 后端的职责
从这里不难看出**后端的职责**：**数据存储**（包括用户账号密码、文章、等）、**身份认证**（来访问的用户是谁）、**权限校验**（该用户是否有进行如此操作的权限）

# 2. Cookie
~~诶！小饼干🍪~~
在访问网站时偶尔会看到
>“您是否接受这些cookie以接受更个性化的广告？”，那么cookie究竟是什么？

结合前文的流程不难想出，cookie类似于**身份令牌**，它让服务器知道发起请求的是哪个用户
打个比方：
日常生活中，你看见朋友走在前面，叫一声“朋友”，朋友不用回头就知道是你，因为他记得你的音色。这个“**记得音色**”，就是**有状态**——朋友脑子里存了你的信息
但 HTTP 服务器默认没有这种记忆。你每次叫它，它都不记得你的音色，只能让你把脸转过来，或者出示工牌，才能确认你是谁
放到 HTTP 里：
- 每个请求默认都“长得差不多”，服务器无法仅凭请求本身知道你是谁
- 所以需要 Cookie，Cookie 就像你每次请求都带在身上的**脸 / 工牌 / 会员卡**
- 浏览器会自动帮你带上 Cookie，服务器一看：哦，原来你是用户 A

但cookie的作用**不止是身份认证**，否则它该叫authentic token了，而非易使人误解的cookie
~~接受所有小饼干🍪！~~

## 2.1 什么是Cookie
**Cookie 是 HTTP 用来“记住状态”的一种机制**。
HTTP 本身是**无状态协议**。
什么叫无状态？就是服务器默认不会在两次请求之间记住你。每个请求都是独立的，第一次请求和第二次请求，在服务器眼里可能只是两个互不相干的陌生人
为什么要设计成无状态的呢？——记住每一个用户的状态会给服务器造成**巨大的负担**
![[image/为什么http是无状态的.png]]


所以：**HTTP 无状态，Cookie 给它加记忆。**
>类似的还有session、token、JWT
## 2.2 Cookie的作用
登录场景下是让服务器知道登陆的用户身份
精确来讲作用是**留存状态**

## 2.3 Cookie的性质
- 是键值对，例如 `sessionid=abc123`。
>键值对，即`key-value`，是一种唯一键指向特定的值的数据结构
- 可由服务器通过 `Set-Cookie`（一种响应头） 下发。
- 浏览器会根据域名、路径自动携带。
- 可以设置过期时间。
>最常见的莫过于`登录过期，请重新登录`

# 3. 爬虫
## 3.1 什么是爬虫
**网络爬虫**是一种自动化程序，能够模拟用户浏览器行为，向目标网站发送请求，获取网页内容并提取有用信息。

## 3.2 爬虫的原理
服务器返回的响应包含了需要的内容
爬虫拿到响应后，解析 HTML、JSON，提取数据并保存

## 3.3 爬虫的流程
1. 爬虫程序向目标服务器发起请求
2. 目标服务器返回响应
3. 爬虫程序自动从响应中获取需要的内容
4. 爬虫完成，保存或分析数据

# 4. 神秘小按钮F12
打开 F12，可以打开开发者工具，能查看元素、网络、手机版网站等
## 4.1 元素 Elements
在这里能看到网站内的内容，也就是DOM结构

## 4.2 网络
能查看请求-响应

### 4.2.1 请求
```http
请求行
请求头
空行
请求体（可选）
```

示例：

```http
GET /articles/1 HTTP/1.1
Host: example.com
Cookie: sessionid=abc123
User-Agent: Mozilla/5.0
```

`请求行`：请求方法 请求路径 http版本
`请求头`： 许多许多键值对

常见请求方法：
GET、PUT、POST、PATCH、DELETE、OPTIONS...
常见请求头:
`Host` 目标域名
`User-Agent` 客户端身份
`Cookie` 身份凭证
`Authorization` 授权令牌
### 4.2.2 响应
```http
状态行
响应头
空行
响应体（可选）
```

示例：
```http
HTTP/1.1 200 OK
Content-Type: text/html
Set-Cookie: sessionid=abc123
<html>...</html>
```

`状态栏`：http协议 状态码 原因短语
`响应头`：许多许多键值对

由状态码快速判断原因：
`2xx`：成功
`4xx`：客户端问题
`5xx`：服务器问题

# 5. 来成为go学长吧

## 5.1 go语言的简单介绍
木犀团队后端组的**主要语言**是**GO**，也叫**GOlang**，是 Google 在 2009 年发布的一门编程语言
因为后端经常要处理：
- 很多请求**同时**进来
- 要快
- 要稳
- 要容易部署
- 要容易维护
Go 正好在这些方面很能打：
- 并发模型简单，不用被线程搞晕
- 编译快，开发体验好
- 运行快，内存占用相对可控
- 交叉编译方便，Linux、Windows、Mac 都能编
- 标准库自带 HTTP、JSON、加密、文件等常用功能
## 5.2 go的吉祥物
go的吉祥物是一只**蓝色地鼠**，其创作者是**Renee French**，**代表Go语言的友善、勤奋和社交特性**![[image/go的吉祥物.png]]

## 5.3 go环境配置
接下来将**安装 vscode 和 golang 环境**

### 5.3.1安装Visual Studio Code

1. 打开[Visual Studio Code官网](https://code.visualstudio.com/)
![[image/vscode官网截图.png]]
点击**Download for Windows**，即可自动下载安装包
2. 打开安装包，选择**我同意此协议**，随后**下一步**
![[image/安装vscode第一步.png]]

3. 选择安装路径，**看个人意愿**，随后下一步
![[image/安装vscode第二步.png]]

4. ![[image/安装vscode第三步.png]]

5. **勾选创建桌面快捷方式**，随后下一步![[image/安装vscode第四步.png]]

6. 总览安装设置，随后**安装**![[image/安装vscode第五步.png]]

7. 等待安装完成即可![[image/安装vscode第六步.png]]
>感兴趣的同学可以去看看jetbrain出品的针对go的IDE——GoLand，这是官网[JetBrains: 软件开发者和团队的必备工具](https://www.jetbrains.com/zh-cn/)，学会获取学生福利也是一项重要技能，可以自行查询相关信息
### 5.3.2 为vscode添加汉化插件
1. 打开vscode![[image/vscode主界面.png]]

2. **左侧导航栏**，四个小正方形组成的图标，**扩展(externsion)**
![[image/扩展图标样式.png]]

3. 在搜索导航栏中搜索**chinese**![[image/扩展界面.png]]
4. 选中喜好的简体或繁体，**Install**![[image/简体中文汉化插件界面.png]]

5. 右下角会弹出弹窗，选中重启**Change Language and Restart**
![[image/弹窗重启.png]]

### 5.3.3 安装go环境
这一步**强烈建议**看直播回放一步一步走或询问AI帮助安装
1. 打开[Go官网](https://golang.google.cn/)，点击**Download**![[image/go官网界面.png]]

2. 在**Featured downloads**选择**自己电脑的系统版本**，即可开始下载安装包![[image/go下载界面.png]]

3. 打开安装包，需要**等待一会**，直到**NEXT**亮起后点击**NEXT**![[image/go安装第一步.png]]

4. 直接**Next**即可![[image/go安装第二步.png]]

5. **设置安装路径**，依据个人喜好即可，但一定要**记住安装在哪**，后续会用上![[image/go安装第三步.png]]

6. 检查安装路径![[image/go安装第四步.png]]

7. 点击**Install**![[image/go安装第五步.png]]

8. 等待安装完成![[image/go安装第六步.png]]

9. **Win+R**，输入sysdm.cpl打开系统属性![[image\运行.png]]

10. 上方导航栏选择**高级**标签，选择右下角的**环境变量**![[image\系统属性.png]]

11. 选择**系统变量**下的**新建**，变量名输入**GOROOT**（注意大写），变量值填入**安装go的目录**，如前文安装在了D:\Dev\Languages\GOLang，则此处输入D:\Dev\Languages\GOLang
12. 选择**系统变量**下的**新建**，变量名输入**GOPATH**（注意大写），变量值填入**非安装go的目录**，例如D:\Dev\Languages\GOPATH
13. 选择**用户变量**下的**新建**，选择**已有变量**PATH，选择**编辑**，在新的窗口里选择**新建**，输入%GOPATH%\bin（这里的%GOPATH%是个占位符，指你的GOPATH目录，以教程为例则为D:\Dev\Languages\GOPATH\bin）

### 5.3.4 为vscode添加go扩展
在vscode中添加go扩展能实现vscode自动帮你审查语法错误、智能填充，在vscode扩展界面搜索GO即可![[扩展页面go扩展.png]]

## 5.4 第一个go程序——Hello World!
hello world 程序几乎是所有语言入门第一个示例程序，它的功能非常简单，就是在屏幕上输出 "Hello, World!" 这句话
从这里，开始你的go学习之旅吧！
```go
package main

import "fmt"

func main(){
	fmt.Print("Hello World!")
}
```

## 5.5 看一下用go发起一个http请求的代码
```go
package main
import (
	"fmt"
	"io"
	"net/http"
	"os"
	"time"
)
func main() {
	// 1. 设置目标网址
	url := "https://www.example.com"
	// 2. 创建一个带超时的 Client
	//    不用 http.Get 是因为它没有超时，网络不好时会一直卡住
	client := &http.Client{
		Timeout: 10 * time.Second,
	}
	// 3. 创建请求
	req, err := http.NewRequest(http.MethodGet, url, nil)
	if err != nil {
		fmt.Println("创建请求失败:", err)
		os.Exit(1)
	}
	// 4. 添加请求头（有些网站会检查 User-Agent，没有会被拒绝）
	req.Header.Set("User-Agent", "Mozilla/5.0 (Go learning bot)")
	// 5. 发起请求
	resp, err := client.Do(req)
	if err != nil {
		fmt.Println("请求失败:", err)
		os.Exit(1)
	}
	// 6. 一定要记得关闭响应体，否则会泄漏连接
	defer resp.Body.Close()
	// 7. 查看状态码
	fmt.Println("状态码:", resp.StatusCode, resp.Status)
	// 8. 读取响应内容
	body, err := io.ReadAll(resp.Body)
	if err != nil {
		fmt.Println("读取响应失败:", err)
		os.Exit(1)
	}
	// 9. 打印前 500 个字符，防止输出太长
	if len(body) > 500 {
		fmt.Println(string(body[:500]))
	} else {
		fmt.Println(string(body))
	}
```